import { ControlConfigInjectorPipe } from './control-config-injector.pipe';

describe('ControlConfigInjectorPipe', () => {
  it('create an instance', () => {
    const pipe = new ControlConfigInjectorPipe();
    expect(pipe).toBeTruthy();
  });
});
